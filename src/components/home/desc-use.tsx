import React from 'react'

const DescUseApi = () => {
    return (
        <section className='flex flex-col gap-4 max-w-4xl'>
            <h2 className='text-default text-3xl'>Quando usar</h2>
            <p className='text-gray-800 font-semibold'>
                PUBLIC JSON é uma API REST online gratuita que você pode usar sempre que você precisar de dados falsos. Pode ser em um README no GitHub, para uma demonstração no CodeSandbox, em exemplos de código no Stack Overflow, ...ou simplesmente para testar as coisas localmente.
            </p>
        </section>
    )
}

export default DescUseApi